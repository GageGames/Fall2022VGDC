using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using DG.Tweening;

public class TWEENPRACTICING : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
		//transform.DOMove(new Vector3(10,2,2), .5f).SetLoops(-1, LoopType.Yoyo ).SetEase(Ease.InBounce);
		//transform.DOScale(new Vector3(5,5,5), 5).SetLoops(-1, LoopType.YoYo);
		//-1 is infinite
		//new Vector
    }

    // Update is called once per frame
   // void Update()
   // {
        // dont do tweening in update, only in start
    //}
}
